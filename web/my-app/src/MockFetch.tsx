//try to do mock fetching

import { type } from '@testing-library/user-event/dist/type';

type mockProps = {
    mId: string,
    mTitle: string,
};
const heroku_url = "https://gentle-bastion-60274.herokuapp.com/messages";


export default async function MockFetch(string: typeof heroku_url){
    

}